Task 6 — Store normalized_host on upsert
==========================================
Date: 2026-06-15

Changes:
- internal/handlers/scan.go: upsertTarget now calls scanner.NormalizeHost(host)
  and includes normalized_host in the INSERT/ON CONFLICT query.

Verification:
- go build ./...  → exit 0
- go test ./...   → all pass (scanner: ok, rest: no test files)
- go vet ./...    → exit 0

Evidence:
- scanner.NormalizeHost("https://www.Example.com/foo") returns "example.com"
- The INSERT now writes both host and normalized_host columns.
- ON CONFLICT (host) DO UPDATE SET normalized_host = EXCLUDED.normalized_host
  keeps the unique constraint on raw host unchanged.
